# Scripts

The tracked script layout is grouped by purpose:

- `benchmark/`: performance comparisons and latency/throughput benchmarks
- `eval/`: evaluation scripts for model quality and output validation
- `quantization/`: quantization and engine-building helpers

Obsolete `middleware-1.py` and `middleware-2.py` have been removed.
